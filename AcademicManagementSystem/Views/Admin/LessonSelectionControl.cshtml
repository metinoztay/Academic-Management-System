
@{
    ViewData["Title"] = "LessonSelectionControl";
    Layout = "~/Views/_AdminLayout.cshtml";
}

<div class="card mb-4">
    <h5 class="card-header">Course Selection Authorize</h5>
                    <div class="card-body">
                      <div>
                        <div class="mb-3">
                            <form method="post">
                    <select name="courseSelection" class="form-select" id="exampleFormControlSelect1" aria-label="Default select example">
                          <option selected="">Select Authorize</option>
                          <option value="1">Course Selection Open</option>
                        <option value="0">Course Selection Close</option>
                        </select>
                            <button type="submit" class="btn btn-primary">Save</button>
                        </form>
                      </div>
                      </div>
                    </div>
                  </div>

